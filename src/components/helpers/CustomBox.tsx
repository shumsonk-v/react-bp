import styled from 'styled-components';
import { spacing } from '@material-ui/system';

const CustomBox = styled.div`${spacing}`;
const CustomSpan = styled.span`${spacing}`;

export { CustomBox, CustomSpan };